# 🤖 Telegram бот "Tag All" — упоминание всех участников группы

Бот позволяет упомянуть всех участников группы одной командой.

## 📋 Возможности

- `/all` или `@all` — упомянуть всех участников группы
- Автоматическое отслеживание участников по сообщениям
- Поддержка пользователей без username (через ссылку на профиль)
- Автоматическое добавление/удаление участников при входе/выходе

---

## 🔧 Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `My Tag All Bot`)
4. Введите username бота (например: `my_tag_all_bot`) — должен заканчиваться на `bot`
5. **Скопируйте токен** — он выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### Настройка прав бота

Отправьте BotFather команду `/mybots`, выберите бота, затем:
- **Bot Settings** → **Group Privacy** → **Turn off** (чтобы бот видел все сообщения)

---

## 🖥️ Шаг 2: Локальный запуск (для теста)

```bash
# Клонируйте или скачайте файлы
# Установите зависимости
pip install -r requirements.txt

# Установите токен
export BOT_TOKEN="ваш_токен_от_BotFather"

# Запустите бота
python bot.py
```

---

## ☁️ Шаг 3: Бесплатный хостинг

### Вариант 1: Railway.app ⭐ (рекомендуется)

**Бесплатно:** $5 кредитов в месяц (хватает на небольшого бота)

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Нажмите **New Project** → **Deploy from GitHub repo**
3. Загрузите код на GitHub и подключите репозиторий
4. Перейдите в **Variables** и добавьте:
   ```
   BOT_TOKEN = ваш_токен
   ```
5. Бот автоматически запустится!

**Или через CLI:**
```bash
npm install -g @railway/cli
railway login
railway init
railway up
railway variables set BOT_TOKEN="ваш_токен"
```

---

### Вариант 2: Render.com

**Бесплатно:** Полностью бесплатный тариф (засыпает после 15 мин неактивности)

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Нажмите **New** → **Web Service**
3. Подключите GitHub репозиторий
4. Настройки:
   - **Environment:** Python
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`
5. Добавьте переменную окружения `BOT_TOKEN`

---

### Вариант 3: Fly.io

**Бесплатно:** 3 маленьких VM бесплатно

1. Установите flyctl:
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. Создайте `fly.toml`:
   ```toml
   app = "my-tag-all-bot"
   primary_region = "ams"

   [build]
     builder = "paketobuildpacks/builder:base"

   [env]
     PORT = "8080"
   ```

3. Создайте `Procfile`:
   ```
   worker: python bot.py
   ```

4. Деплой:
   ```bash
   fly auth login
   fly launch
   fly secrets set BOT_TOKEN="ваш_токен"
   fly deploy
   ```

---

### Вариант 4: PythonAnywhere

**Бесплатно:** Базовый аккаунт (с ограничениями)

1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Перейдите в **Consoles** → **Bash**
3. Загрузите файлы:
   ```bash
   git clone https://github.com/ваш_репозиторий.git
   cd ваш_репозиторий
   pip install --user -r requirements.txt
   ```
4. Создайте **Scheduled Task** (Tasks):
   ```bash
   export BOT_TOKEN="ваш_токен" && python /home/username/bot.py
   ```

⚠️ **Минус:** На бесплатном тарифе есть ограничения на внешние запросы.

---

### Вариант 5: Replit

**Бесплатно:** Есть бесплатный тариф

1. Зайдите на [replit.com](https://replit.com)
2. Создайте новый **Python** проект
3. Загрузите файлы `bot.py` и `requirements.txt`
4. В разделе **Secrets** добавьте `BOT_TOKEN`
5. Нажмите **Run**

⚠️ **Минус:** Бесплатные проекты засыпают, нужен UptimeRobot для пинга.

---

### Вариант 6: Koyeb

**Бесплатно:** 1 микро-инстанс

1. Зарегистрируйтесь на [koyeb.com](https://www.koyeb.com)
2. **Create App** → **GitHub**
3. Выберите репозиторий
4. Настройки:
   - **Builder:** Buildpack
   - **Run command:** `python bot.py`
5. Добавьте переменную `BOT_TOKEN`

---

## 📁 Структура файлов для деплоя

```
telegram-tag-all-bot/
├── bot.py              # Основной код бота
├── requirements.txt    # Зависимости Python
├── Procfile           # Для Heroku/Fly.io (опционально)
└── README.md          # Этот файл
```

### Procfile (если нужен):
```
worker: python bot.py
```

---

## 🚀 Использование бота

1. Добавьте бота в группу
2. Сделайте бота **администратором** группы
3. Подождите, пока участники напишут хотя бы одно сообщение
4. Используйте `/all` или просто напишите `@all` в сообщении

---

## ❓ FAQ

**Q: Бот не видит всех участников?**  
A: Бот запоминает участников только после того, как они напишут сообщение. Это ограничение Telegram API.

**Q: Как сбросить список участников?**  
A: Перезапустите бота. Список хранится в памяти.

**Q: Можно ли сохранять участников в базу данных?**  
A: Да, код можно доработать, добавив SQLite или Redis.

---

## 📝 Лицензия

MIT — используйте свободно!
